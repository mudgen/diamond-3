{
  "extends": "solhint:default",
  "plugins": [ "prettier" ],
  "rules": {
    "prettier/prettier": [ "error", { "endOfLine": "auto" } ],
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "max-line-length": [
      "warn",
      120
    ]
  }
}
